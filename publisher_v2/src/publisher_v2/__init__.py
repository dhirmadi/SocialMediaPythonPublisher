"""Publisher V2 — the data plane: config, orchestration, services, utils and web UI.

Deliberately empty at the top level (``__all__ = []``): import from the
subpackages (``publisher_v2.config``, ``.core``, ``.services``, ``.utils``,
``.web``) so importing the package pulls in no optional dependencies.
"""

__all__ = []
