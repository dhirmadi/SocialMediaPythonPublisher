## Story workflow commands — fully archived

This repo uses a **flat roadmap** model. Roadmap items live at `docs_v2/roadmap/PUB-NNN_slug.md`.

**Use these instead:**

- `/product/propose-item` + `/product/harden` (Cursor) — create and harden a roadmap item
- Claude Code's `/implement` + `/verify` — plan + implement a roadmap item via TDD with quality gates

The `feature/00_defineitem.md` / `feature/00_implementitem.md` commands this file
used to point to are themselves archived (see `_archived/feature/README.md`) —
this whole directory, including its own nested `_archived/`, is historical only.
